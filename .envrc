# Prioritize system libraries over Homebrew for Flutter Linux builds
export PKG_CONFIG_PATH=/usr/lib/x86_64-linux-gnu/pkgconfig:/usr/share/pkgconfig:$PKG_CONFIG_PATH

# Add any other project-specific environment variables here
export FLUTTER_PROJECT=true

# Uncomment to completely remove Homebrew from PATH if needed
# export PATH=$(echo $PATH | tr ':' '\n' | grep -v "linuxbrew" | tr '\n' ':') 